<svg xmlns="http://www.w3.org/2000/svg" width="350" height="80" viewBox="0 0 350 80">
    <!-- Background (optional, can be removed if not needed) -->
    <rect width="350" height="80" opacity="0"/>

    <!-- Text "exerc" -->
    <text x="20" y="60" font-family="Arial, sans-serif" font-size="60" fill="#333">exerc</text>

    <!-- Highlighted "AI" -->
    <text x="170" y="60" font-family="Arial, sans-serif" font-size="60" fill="#FF5722" font-weight="bold">AI</text>

    <!-- Remaining "ses" -->
    <text x="230" y="60" font-family="Arial, sans-serif" font-size="60" fill="#333">ses</text>
</svg>
